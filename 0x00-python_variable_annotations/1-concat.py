#!/usr/bin/env python3
"""Module that contains a single function that concatenate two strings"""


def concat(str1: str, str2: str) -> str:
    """concatenate two strings"""
    return "{0}{1}".format(str1, str2)
